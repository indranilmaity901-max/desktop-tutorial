services:
  - type: web
    name: wpacs-dashboard
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python scripts/init_postgres.py && python live_server.py"
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: DATABASE_URL
        sync: false
      - key: PUBLIC_URL
        value: https://dashboard.wpacs.com
